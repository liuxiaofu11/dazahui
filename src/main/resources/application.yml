server:
  port: 1111
spring:
  datasource:
    druid:
      db-type: com.alibaba.druid.pool.DruidDataSource
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://139.224.115.130:3306/ceshi?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
      username: root
      password: root
      min-idle: 5
      max-active: 20
      max-wait: 30000
  data:
    elasticsearch:
      cluster-name: elasticsearch
      cluster-nodes: localhost:9300
    mongodb:
      uri: mongodb://localhost:27017/mongodb
  thymeleaf:
    cache: false
  aop:
    proxy-target-class: true
  cache:
    type: redis
  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: admin
    publisher-confirm: true # 如果对异步消息需要回调必须设置为true
mybatis:
  type-aliases-package: com.tengda.dazahui.system.domian
  mapper-locations: classpath:mapper/*/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl



